package ca.tarasyk.navigator.pathfinding.path.movement;

public class MoveConstants {
    // D * (dx + dy + dz) where D is the lowest cost distance between nodes
    public static final double MIN_ADMISSIBLE_DISTANCE = 1.0;
}
